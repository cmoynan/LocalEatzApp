from django.apps import AppConfig

# Configuration for the 'restaurants' application
class RestaurantsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'restaurants'
